import 'package:flutter/material.dart';
import '../../../../../shared/widgets/mior_sc/gallery.dart';


class AccessoriesGalleryScreen extends StatelessWidget {
  AccessoriesGalleryScreen({Key? key, required this.txt}) : super(key: key);

  final String txt;
  @override
  Widget build(BuildContext context) {
    return GalleryWidget(dataStream(txt));
  }
}


class BagsGalleryScreen extends StatelessWidget {
  BagsGalleryScreen({Key? key, required this.txt}) : super(key: key);

  final String txt;
  @override
  Widget build(BuildContext context) {
    return GalleryWidget(dataStream(txt));
  }
}


class BeautyGalleryScreen extends StatelessWidget {
  BeautyGalleryScreen({Key? key, required this.txt}) : super(key: key);

  final String txt;
  @override
  Widget build(BuildContext context) {
    return GalleryWidget(dataStream(txt));
  }
}


class ElectronicsGalleryScreen extends StatelessWidget {
  ElectronicsGalleryScreen({Key? key, required this.txt}) : super(key: key);

  final String txt;
  @override
  Widget build(BuildContext context) {
    return GalleryWidget(dataStream(txt));
  }
}


class HomeGardenGalleryScreen extends StatelessWidget {
  HomeGardenGalleryScreen({Key? key, required this.txt}) : super(key: key);

  final String txt;
  @override
  Widget build(BuildContext context) {
    return GalleryWidget(dataStream(txt));
  }
}


class KidsGalleryScreen extends StatelessWidget {
  KidsGalleryScreen({Key? key, required this.txt}) : super(key: key);

  final String txt;
  @override
  Widget build(BuildContext context) {
    return GalleryWidget(dataStream(txt));
  }
}


class MenGalleryScreen extends StatelessWidget {
  MenGalleryScreen({Key? key, required this.txt}) : super(key: key);

  final String txt;
  @override
  Widget build(BuildContext context) {
    return GalleryWidget(dataStream(txt));
  }
}


class ShoesGalleryScreen extends StatelessWidget {
  ShoesGalleryScreen({Key? key, required this.txt}) : super(key: key);

  final String txt;
  @override
  Widget build(BuildContext context) {
    return GalleryWidget(dataStream(txt));
  }
}


class WomenGalleryScreen extends StatelessWidget {
  WomenGalleryScreen({Key? key, required this.txt}) : super(key: key);

  final String txt;
  @override
  Widget build(BuildContext context) {
    return GalleryWidget(dataStream(txt));
  }
}

